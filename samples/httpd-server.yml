# httpd-server.yml

includes: [base]
machines:
  web:
    inherit: base
    provisions:
      - kind: shell
        name: basics
        inline: |
          apt-get update
          apt-get install -y git gnupg
          apt-get install -y lamp-server
